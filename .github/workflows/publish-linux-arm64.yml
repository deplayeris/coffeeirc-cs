name: Publish Linux ARM64

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  publish-linux-arm64:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
    
    - name: Install NativeAOT dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y clang zlib1g-dev
    
    - name: Install Cake.Tool
      run: dotnet tool install -g Cake.Tool --version 4.0.0
    
    - name: Publish and Package Linux ARM64
      run: dotnet cake --target=Package-Linux-Arm64
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: coffeeirc-linux-arm64
        path: artifacts/coffeeirc-linux-arm64.tar.gz
        retention-days: 730
